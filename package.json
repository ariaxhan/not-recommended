{
  "name": "not-recommended",
  "version": "0.1.0",
  "private": true,
  "description": "A question-first, non-personalized YouTube extension.",
  "scripts": {
    "test": "node --test",
    "lint": "node scripts/check.mjs",
    "verify": "npm run test && npm run lint",
    "package": "node scripts/package.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
